--[[
  KESH V1.0.8
  
  This is the KESH assembly file, this loads the language engine aswell as the code project to make the stuff run

  Do not touch anything in here, it may break all of KESH!
]]
require "codelibrary.Engine" --This loads the external language
require "Code" --Loads your project
--After this you can add more code files by doing require "FileName" with no extension!